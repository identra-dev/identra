{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults, plus the two window permissions Identra needs to close itself. core:window:default is read-only: it grants is-closable but not close, and not destroy, so without these two the app cannot be quit from inside itself at all.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "updater:default",
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}